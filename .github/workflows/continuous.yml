# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_continuous --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: continuous-integration

on:
  push:
    branches:
      - main
      - develop
      - 'releases/**'
      - 'feature/**'
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+-*'
  pull_request:
    branches:
      - 'releases/**'

jobs:
  ubuntu:
    name: continuous-integration-ubuntu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v5
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: Setup latest dotnet sdk
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0
      - name: Install .NET runtimes (6.0, 8.0)
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            6.0.x
            8.0.x
          package-type: runtime # Use 'aspnetcore-runtime' if you need ASP.NET Core specifically
      - name: 'Run: ensure build.cmd script to be executable'
        run: chmod +x ./build.cmd
      - name: 'Run: ContinuousIntegration'
        run: ./build.cmd ContinuousIntegration
        env:
          NuGetApiKey: ${{ secrets.NUGET_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COVERALLS_REPO_TOKEN: ${{ secrets.COVERALLS_REPO_TOKEN }}
      - name: 'Publish: ConfirmSteps.Net build artefacts'
        uses: actions/upload-artifact@v7
        with:
          name: ConfirmSteps.Net-build-artefacts
          path: .build/publish/nuget
